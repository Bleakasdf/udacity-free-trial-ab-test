# Power BI dashboard

Open `project/UdacityABTest.pbip` in Power BI Desktop.

Before refresh, set the `DataFolder` parameter to the local `data/powerbi` folder. The report is organized as a decision flow:

1. Decision
2. Metric evidence
3. Experiment checks
